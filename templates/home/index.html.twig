{% extends 'base.html.twig' %}

{% block title %}SmartHome Prototype – Übersicht{% endblock %}

{% block stylesheets %}
  {{ parent() }}
  <link rel="stylesheet" href="{{ asset('css/home.css') }}">
{% endblock %}

{% block body %}
{% set currentPage = 'home' %}

<div class="page page--home">
  <header class="page__header">
    <a href="{{ path('app_home') }}" class="page__logo">
      <span class="page__logo-mark">SH</span>
      <span class="page__logo-text">
        <strong>SmartHome</strong>
        <small>Prototype</small>
      </span>
    </a>
    <nav class="page__nav">
      <a href="{{ path('app_home') }}" class="page__nav-link{{ currentPage == 'home' ? ' is-active' : '' }}">Übersicht</a>
      <a href="{{ path('app_status') }}" class="page__nav-link{{ currentPage == 'status' ? ' is-active' : '' }}">Status</a>
    </nav>
  </header>

  <main class="page__main">
    <section class="hero">
      <div class="hero__content">
        <span class="badge">Projektüberblick</span>
        <h1>Digitale Sicht auf dein Raumklima</h1>
        <p>
          Dein Raspberry Pi sendet präzise Sensordaten in Echtzeit an die Datenbank.
          Diese Webapp zeigt sie dir klar, schnell und ästhetisch an.
        </p>
        <div class="hero__actions">
          <a href="{{ path('app_status') }}" class="button button--primary">Live-Daten ansehen</a>
        </div>
      </div>

      <div class="hero__panel">
        <div class="hero__panel-inner">
          <h2>Was macht die Anwendung?</h2>
          <p>Unsere Weboberfläche bildet dein Raumklima digital ab – minimalistisch, aber präzise.</p>
          <ul>
            <li>Erfasst Daten vom Raspberry Pi 5 mit BME280 &amp; TSL2591.</li>
            <li>Speichert sie sicher in der Datenbank und aktualisiert regelmäßig.</li>
            <li>Visualisiert alle Werte übersichtlich – ohne manuelles Nachladen.</li>
          </ul>
        </div>
      </div>
    </section>

    {# Messwerte Karten #}
    <section class="sensors">
      <div class="sensors__header">
        <span class="badge">Messgrößen</span>
        <h2>Diese Werte bereiten wir auf</h2>
        <p>Alle Messungen kommen im Sekundentakt aus der Datenbank und werden unverändert dargestellt.</p>
      </div>

      <div class="sensors__grid">
        <article class="sensor-card">
          <div class="sensor-icon">🌡️</div>
          <h3>Temperatur (°C)</h3>
          <p>Aktuelle Raumtemperatur.</p>
        </article>

        <article class="sensor-card">
          <div class="sensor-icon">💧</div>
          <h3>Relative Luftfeuchtigkeit (%)</h3>
          <p>Wasserdampfanteil der Luft.</p>
        </article>

        <article class="sensor-card">
          <div class="sensor-icon">⚙️</div>
          <h3>Luftdruck (hPa)</h3>
          <p>Atmosphärischer Luftdruck.</p>
        </article>

        <article class="sensor-card">
          <div class="sensor-icon">💡</div>
          <h3>Helligkeit (Lux)</h3>
          <p>Lichtstärke am Sensor.</p>
        </article>
      </div>
    </section>

  </main>
</div>
{% endblock %}